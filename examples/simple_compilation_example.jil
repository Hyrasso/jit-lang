f : fn(u64, u64)u64 = fn (a:u64, b:u64) u64:
    inner_f : fn(u64)u64 = fn(a: u64) u64:
        a + 1
    inner_f(a) + b

res : u64 = f(3, 5)

print(res)

ff: fn(u64, u64, u64) u64 = fn(a: u64,b: u64,c: u64) u64:
    tmp: u64 = a / c
    tmp - b

v: u64 = 1 - 2

print(ff(6, v, 3))
print(ff(6, v, 4))
